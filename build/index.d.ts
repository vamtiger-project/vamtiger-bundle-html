import { MainParams as Params } from './types';
declare const _default: ({ entryFilePath, entryFolderPath, bundleFilePath, copyBundleFilePath, json }: Params) => Promise<boolean>;
export default _default;
